description = 'equal5 core engine library'

configurations {
    testFixtures {
        extendsFrom testRuntime
    }
}

task testsJar(type: Jar, dependsOn: testClasses) {
    classifier = 'tests'
    from sourceSets.test.output.classesDir
}

artifacts {
    testFixtures testsJar
}
